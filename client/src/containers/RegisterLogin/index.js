import React from 'react';

import RegisterLogin from '../../components/RegisterLogin/';

const RegisterLoginContainer = (props) => {
    return <RegisterLogin {...props} />
};

export default RegisterLoginContainer;